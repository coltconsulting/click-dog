apiVersion: v1
kind: Secret
metadata:
  name: click-dog-secret
  namespace: click-dog
  labels:
    app.kubernetes.io/name: click-dog
type: Opaque
stringData:
  # REQUIRED: replace these values or use sealed-secrets / external-secrets in production.
  CLICKHOUSE_USERNAME: "click_dog_monitor"
  # CLICKHOUSE_PASSWORD: "replace-with-monitoring-password"
