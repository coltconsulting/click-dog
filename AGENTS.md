# AGENTS.md — click-dog

click-dog keeps a **single source of truth** for agent and contributor
instructions in [`CLAUDE.md`](./CLAUDE.md). This file is an intentional
pointer, not a copy — duplicating the content here only lets the two drift
out of sync.

**→ Read [`CLAUDE.md`](./CLAUDE.md)** for everything: agent authorization
and completion rules, build and test commands, project structure, code
conventions, execution modes, the git/CI workflow, and key architectural
decisions.
